@startuml
class LocalConversationEntity {
    <<Data Class>>
    + id: String
    + profileJson: String
    + createdAt: Long
    + title: String?
    + dateStarted: Long
    + dateLastSaved: Long
    + messageCount: Int
}
@enduml
